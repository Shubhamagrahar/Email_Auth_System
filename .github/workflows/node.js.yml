on:
  push:
    branches: ["main"]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      # 1. Clone repository
      - name: Clone Repo
        uses: actions/checkout@v3

      # 2. Setup Node.js
      - name: Configure Node.js
        uses: actions/setup-node@v2
        with:
          node-version: "18.x"

      # 3. Install & build frontend
      - name: Install Frontend Dependencies
        run: npm ci
        working-directory: ./frontend

      - name: Build Frontend Project
        run: CI=false npm run build
        working-directory: ./frontend

      # 4. Check build folder (debugging safeguard)
      - name: Verify Frontend Build Output
        run: ls -la ./frontend/build

      # 5. Prepare SSH key
      - name: Prepare SSH Key
        run: |
          echo "${{ secrets.EC2_PRIVATE_KEY }}" > key.pem
          chmod 400 key.pem

      # 6. Copy backend & frontend to EC2
      - name: Copy Files to EC2
        run: |
          scp -o StrictHostKeyChecking=no -i key.pem -r ./backend ubuntu@13.204.109.107:~/backend
          scp -o StrictHostKeyChecking=no -i key.pem -r ./frontend/build ubuntu@13.204.109.107:~/frontend_build

      # 7. Install backend deps & restart app
      - name: Install Backend and Restart PM2
        uses: appleboy/ssh-action@master
        with:
          host: 13.204.109.107
          username: ubuntu
          key: ${{ secrets.EC2_PRIVATE_KEY }}
          port: 22
          script: |
            cd ~/backend
            npm ci
            # start backend using PM2 (adjust entry file if not index.js)
            pm2 start index.js --name backend || pm2 restart backend
            pm2 save
            pm2 status

      # 8. Cleanup key
      - name: Remove private key
        run: rm -f key.pem
